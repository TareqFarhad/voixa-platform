{
  "name": "voixa-platform",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --filter @voixa/web dev\" \"pnpm --filter @voixa/api dev\" \"pnpm --filter @voixa/worker dev\"",
    "dev:web": "pnpm --filter @voixa/web dev",
    "dev:api": "pnpm --filter @voixa/api dev",
    "dev:worker": "pnpm --filter @voixa/worker dev",
    "build": "pnpm -r --if-present build",
    "build:pages": "NEXT_PUBLIC_STATIC_EXPORT=1 NEXT_PUBLIC_BASE_PATH=/voixa-platform NEXT_PUBLIC_API_URL='' pnpm --filter @voixa/web build && rm -rf docs && mkdir -p docs && cp -r apps/web/out/. docs/ && touch docs/.nojekyll",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
