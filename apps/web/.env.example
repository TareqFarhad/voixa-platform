NEXT_PUBLIC_API_URL=http://localhost:4000
# Set to "1" when building the static landing page for GitHub Pages.
# This switches Next.js into export mode and prefixes routes with the
# repository basePath defined in next.config.mjs.
NEXT_PUBLIC_STATIC_EXPORT=
NEXT_PUBLIC_BASE_PATH=
