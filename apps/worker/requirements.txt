pydantic>=2.9.0
python-dotenv>=1.0.1
redis>=5.0.7
requests>=2.32.3
# Optional audio stack - the worker degrades gracefully when these are missing
# and falls back to ffmpeg based processing so the scaffold runs anywhere.
numpy>=1.26.4
soundfile>=0.12.1
librosa>=0.10.2
pydub>=0.25.1
