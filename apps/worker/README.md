# Voixa Worker

Python microservice scaffold for asynchronous audio processing jobs.

## Current scope
- health command
- pipeline stage placeholders
- typed job payload contract

## Run
```bash
python -m voixa_worker.main
```
